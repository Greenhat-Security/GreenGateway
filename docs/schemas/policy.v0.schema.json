{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greengateway.dev/schemas/policy.v0.schema.json",
  "title": "GreenGateway RBAC Policy",
  "description": "Draft 2020-12 JSON Schema for the GreenGateway RBAC policy document. Unknown top-level keys are permitted for forward compatibility and will produce a startup warning.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "schema_version"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^0\\."
    },
    "id": {
      "type": "string"
    },
    "default_action": {
      "enum": [
        "allow",
        "deny"
      ]
    },
    "enforcement_mode": {
      "$ref": "#/$defs/enforcement_mode"
    },
    "roles": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/role_entry"
      }
    },
    "routes": {
      "type": "array",
      "description": "Ordered route-to-permission rules. Rules are evaluated in array order, and the first matching rule wins.",
      "items": {
        "$ref": "#/$defs/route_rule"
      }
    },
    "rules": {
      "type": "array",
      "description": "Ordered direct firewall rules for HTTP paths or exact MCP tool names. Rules are evaluated in array order within their target type, and the first matching rule wins. HTTP path rules are additive alongside routes and do not replace route-to-permission rules. MCP tool-name rules are an additional restriction layer alongside policy.tools.*.allowed_roles.",
      "items": {
        "$ref": "#/$defs/rule"
      }
    },
    "egress": {
      "$ref": "#/$defs/egress_policy",
      "description": "Policy-driven outbound egress rules layered on top of EGRESS_ALLOWED_HOSTS and auto-seeded infrastructure endpoint hosts. Host patterns are additive. CIDRs explicitly allow matching private resolved IPs through EGRESS_DENY_PRIVATE_IPS=true; private IPs outside listed CIDRs remain blocked. If ports is non-empty, the destination port must be listed."
    },
    "rate_limits": {
      "type": "array",
      "description": "Ordered rate-limit override rules. Rules are evaluated in array order, and the first matching rule wins. This is an additional constraint layered on top of the always-enforced, pre-authentication RATE_LIMIT_READ_*/RATE_LIMIT_WRITE_* lanes, not a replacement for them: a matching rule applies only to authenticated requests with a validated principal, and the global lanes still apply to every request regardless of whether a rule matches.",
      "items": {
        "$ref": "#/$defs/rate_limit_rule"
      }
    },
    "tools": {
      "type": "object",
      "description": "Optional per-tool runtime policy for generic MCP tool invocation admission. Each key is a tool name. Tool implementations and registry wiring are configured separately; this section controls generic enablement, role admission, timeout, and per-tool concurrency.",
      "additionalProperties": {
        "$ref": "#/$defs/tool_policy_entry"
      }
    }
  },
  "$defs": {
    "enforcement_mode": {
      "enum": [
        "enforce",
        "shadow"
      ]
    },
    "role_entry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "permissions": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "route_rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path_prefix",
        "permission"
      ],
      "properties": {
        "methods": {
          "type": "array",
          "description": "HTTP methods this rule matches. Empty or [\"*\"] means any method.",
          "items": {
            "type": "string"
          }
        },
        "path_prefix": {
          "type": "string",
          "description": "Path prefix matched with starts_with, such as /admin or /v1/."
        },
        "permission": {
          "type": "string",
          "description": "Permission required to access a matching route."
        },
        "enforcement_mode": {
          "$ref": "#/$defs/enforcement_mode",
          "description": "Optional rule-level override for whether deny decisions block or emit authz.would_deny and forward."
        }
      }
    },
    "principal_matcher": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional principal constraints for a rule. Non-empty fields are ANDed together: roles AND auth_methods AND principal_ids must all match when configured. Within each field, any listed value matches. An empty object or omitted principal matcher means any principal, authenticated or not.",
      "properties": {
        "roles": {
          "type": "array",
          "description": "Role names this rule matches. Empty or omitted means any role set.",
          "items": {
            "type": "string"
          }
        },
        "auth_methods": {
          "type": "array",
          "description": "Authentication methods this rule matches. Empty or omitted means any authentication method.",
          "items": {
            "enum": [
              "bearer_token",
              "session_cookie",
              "service_token"
            ]
          }
        },
        "principal_ids": {
          "type": "array",
          "description": "Exact principal user_id values this rule matches. Empty or omitted means any principal id.",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "rule_action": {
      "description": "For HTTP path rules, direct rules run before, and take precedence over, the routes/permission model: the first matching path rule is the sole authority for the request, and routes are never consulted once a path rule matches. For MCP tool-name rules, direct rules are an additional restriction layered after per-tool allowed_roles; 'allow' and 'shadow' do not override a failed allowed_roles check. 'deny' blocks, while 'shadow' permits the rule layer but records a would-deny observation event for policy-authoring dry runs.",
      "enum": [
        "allow",
        "deny",
        "shadow"
      ]
    },
    "rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "action"
      ],
      "oneOf": [
        {
          "required": [
            "path"
          ],
          "not": {
            "required": [
              "tool_name"
            ]
          }
        },
        {
          "required": [
            "tool_name"
          ],
          "not": {
            "required": [
              "path"
            ]
          }
        }
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Optional stable identifier for audit and observation attribution. When omitted, live evaluation reports the rule's array index."
        },
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this direct firewall rule participates in live request evaluation. Omitted rules default to true for backward compatibility. A false rule is skipped entirely as if it were absent, so evaluation falls through to the next rule or the policy default."
        },
        "methods": {
          "type": "array",
          "description": "HTTP methods this rule matches. Empty or [\"*\"] means any method.",
          "items": {
            "type": "string"
          }
        },
        "path": {
          "type": "string",
          "pattern": "^/",
          "description": "Absolute HTTP path pattern matched against the whole request path. Exactly one of path or tool_name must be set. Syntax is segment-based and anchored, never substring-based. Literal segments match exactly and case-sensitively. * matches exactly one non-empty path segment. ** matches zero or more complete path segments. {name} matches exactly one non-empty segment and names the capture for future rule-preview/discovery UI; capture names use ASCII letters, digits, and _, and must start with a letter or _."
        },
        "tool_name": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "description": "Exact MCP tool name matched by this rule. Tool-name rules apply only to MCP tool calls and never to HTTP requests. Exactly one of path or tool_name must be set."
        },
        "principal": {
          "$ref": "#/$defs/principal_matcher"
        },
        "action": {
          "$ref": "#/$defs/rule_action"
        }
      }
    },
    "egress_policy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "hosts": {
          "type": "array",
          "description": "Case-insensitive hostname patterns allowed for outbound egress. A pattern without * matches exactly. A wildcard is only valid as the entire leftmost label in a *. prefix, such as *.example.com; it matches any non-empty DNS label prefix ending in .example.com, including foo.example.com and bar.baz.example.com, but not example.com.",
          "items": {
            "type": "string",
            "pattern": "^(?:\\*\\.)?(?=.{1,253}$)(?:[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?\\.)*[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?$"
          }
        },
        "cidrs": {
          "type": "array",
          "description": "IPv4 or IPv6 CIDR ranges that explicitly allow matching private resolved destination IPs through the private-IP block. These are exceptions to EGRESS_DENY_PRIVATE_IPS, not a global replacement for it.",
          "items": {
            "type": "string",
            "pattern": "^(?:(?:(?:25[0-5]|2[0-4][0-9]|1?[0-9]{1,2})\\.){3}(?:25[0-5]|2[0-4][0-9]|1?[0-9]{1,2})/(?:[0-9]|[12][0-9]|3[0-2])|(?:[0-9A-Fa-f]*:)+[0-9A-Fa-f:.]*/(?:[0-9]|[1-9][0-9]|1[01][0-9]|12[0-8]))$"
          }
        },
        "ports": {
          "type": "array",
          "description": "Allowed outbound destination ports. Empty or absent means no policy-level port restriction.",
          "items": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535
          }
        }
      }
    },
    "rate_limit_rule": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requests_per_second",
        "burst"
      ],
      "properties": {
        "principal": {
          "$ref": "#/$defs/principal_matcher",
          "description": "Optional principal constraints. Empty or omitted means any principal, authenticated or not."
        },
        "methods": {
          "type": "array",
          "description": "HTTP methods this override matches. Empty or [\"*\"] means any method.",
          "items": {
            "type": "string"
          }
        },
        "path": {
          "type": "string",
          "pattern": "^/",
          "description": "Optional absolute path pattern matched against the whole request path. When omitted, any path matches. Syntax is segment-based and anchored, never substring-based. Literal segments match exactly and case-sensitively. * matches exactly one non-empty path segment. ** matches zero or more complete path segments. {name} matches exactly one non-empty segment and names the capture for future rule-preview/discovery UI; capture names use ASCII letters, digits, and _, and must start with a letter or _."
        },
        "requests_per_second": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "Positive finite token refill rate for matching requests, in requests per second."
        },
        "burst": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4294967295,
          "description": "Positive token bucket burst size for matching requests."
        }
      }
    },
    "tool_policy_entry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this tool is available for invocation. Omitted values default to true."
        },
        "allowed_roles": {
          "type": "array",
          "default": [],
          "description": "Role names allowed to invoke this tool. Empty or omitted means no role constraint beyond enabled.",
          "items": {
            "type": "string"
          }
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1,
          "default": 30000,
          "description": "Maximum execution time for one tool invocation, in milliseconds."
        },
        "max_concurrent": {
          "type": "integer",
          "minimum": 0,
          "maximum": 4294967295,
          "default": 8,
          "description": "Maximum concurrent executions for this tool. A configured value of 0 is treated by the runtime as 1 to avoid a zero-permit semaphore."
        }
      }
    }
  }
}
